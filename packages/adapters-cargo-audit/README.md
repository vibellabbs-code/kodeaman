# adapters-cargo-audit

cargo-audit adapter for Rust SCA. It runs `cargo audit --json`, normalizes RustSec advisories, and includes bilingual remediation coaching. Projects are detected from `Cargo.toml` or `Cargo.lock`.
