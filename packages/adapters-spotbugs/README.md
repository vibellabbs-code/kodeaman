# adapters-spotbugs

SpotBugs adapter for Java SAST. It runs `spotbugs -textui -xml -effort:max <jar>` and normalizes bug patterns with bilingual coaching. Projects are detected from Maven or Gradle build files.
